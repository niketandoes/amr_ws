/**:
  ros__parameters:
    use_stamped: false
    topics:
      coordinator:
        topic: cmd_vel_coord
        timeout: 0.5
        priority: 20
      navigation:
        topic: cmd_vel_nav
        timeout: 0.5
        priority: 10
